import {Box} from '@mui/material'
import { styled } from '@mui/material/styles';

const BoxContent = styled(Box)({
    marginTop: '110px',
    backgroundColor: 'rgb(190, 15, 15)'
})

export default BoxContent